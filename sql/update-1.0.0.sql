-- sql/update-1.0.0.sql — placeholder migration. The 1.0.0
-- release is the first installable version, so there is
-- nothing to migrate from a prior schema. Future versions
-- add a file per version (spec § Invariants: "DB
-- migrations").
SELECT 1;
